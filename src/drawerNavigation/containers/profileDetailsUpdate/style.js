import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    skipButton:{
        flexDirection:"row",
        position:"absolute",
        top:"7%",
        right:"0%",
        height:30,
        width:80,
        alignItems:"center",
        justifyContent:"center",
        zIndex:50,
    },
    skipButtonText:{
        color:"#fff",
        fontSize:15
    }
});